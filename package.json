{
  "scripts": {
    "preinstall": "git clone https://github.com/gkkachi/node-slack-sdk && mv node-slack-sdk/packages/events-api . && cd events-api && npm install"
  },
  "dependencies": {
    "@google-cloud/pubsub": "*",
    "events-api": "./events-api"
  }
}
